#pragma once

/// Renders the save/load setup controls inline (no window) for the sidebar.
/// Call from window::render() inside the Sidebar Begin/End, like the checkboxes.
void show_setup_controls();
